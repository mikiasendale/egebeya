prompt 1-1
'''
check if these are done and complete what isn't done 
Replace @webcontainer/api with @codesandbox/sandpack-react in the Pro-tier code editor (/dashboard/code-editor). Do NOT use Nodebox — use Sandpack's standard 
bundler mode only, to stay within its unrestricted commercial-use terms.


1. Replace the WebContainer boot logic with a <Sandpack /> or the lower-level 
   <SandpackProvider> + <SandpackLayout> components, using the "vite-react" or 
   "react" template as the closest match to our starter template.
2. Map our existing pro_site_files (path → content) into Sandpack's `files` prop 
   format on load.
3. Sandpack's own editor + preview components can replace the separate Monaco 
   instance if desired (Sandpack includes its own CodeMirror-based editor), OR 
   keep Monaco and sync its content into Sandpack's file state on change — your 
   choice based on which feels better once you see both.
4. Remove the Cross-Origin-Embedder-Policy / Cross-Origin-Opener-Policy headers 
   added for WebContainer's SharedArrayBuffer requirement — Sandpack's standard 
   bundler mode doesn't need cross-origin isolation. Verify this by testing the 
   editor still works after removing them.
5. Save/Publish button behavior stays the same — still writes to pro_site_files 
   via the existing API endpoints.


Verify: the starter template loads and renders in Sandpack's preview, edits update 
live, and no COEP/COOP-related console errors or broken embedded content appear 
elsewhere in the dashboard now that those headers are removed from this route.Wiring in monitoring, bot protection, an internal admin panel, and audit secrets hygiene. 
Use these credentials (already obtained): SENTRY_DSN=[your DSN], 
TURNSTILE_SITE_KEY=[your key], TURNSTILE_SECRET_KEY=[your secret].


1. Sentry: integrate the Sentry SDK on both backend (Express error middleware) 
   and frontend (React error boundary + uncaught exception capture). Confirm a 
   deliberately-thrown test error actually appears in your Sentry dashboard.


2. Bot protection: add Cloudflare Turnstile to the public booking form 
   (customer info step). Verify the token server-side in 
   POST /api/public/bookings before accepting the booking — reject if missing/
   invalid.


3. Admin panel: build a minimal internal-only dashboard at /admin (gated to a 
   new `is_superadmin` flag on users, NOT the same as tenant owner/staff roles — 
   this should only ever be true for your own account, set manually in the DB 
   for now). Include: list of all tenants with plan/status, ability to 
   suspend/reactivate a tenant (add `is_suspended` to tenants, and make the 
   tenant-resolution middleware refuse to serve suspended tenants' public sites), 
   and basic platform stats (tenant count, booking count).


4. Secrets audit: search the full git history (not just current files) for 
   anything resembling an API key, DB password, or JWT secret ever committed. 
   Confirm .env is in .gitignore and always was. Check the production frontend 
   build output for any leaked secrets. Report anything found — if a real secret 
   was ever exposed, flag it clearly so it can be rotated, don't just fix the 
   leak going forward.


Report what's live and verified working for each of the four items.
'''
prompt 1-2
'''
You are working on the Egebeya project at D:\Workstation\egebeya. Implement three related fixes together since they touch the same area (Pro tier access control).


## Fix A: Real plan/subscription system (currently only a manual DB flag)


1. Add tables:
   - `plans`: id, name ('free', 'pro'), price_etb, max_staff, custom_domain_allowed, 
     description.
   - `tenant_subscriptions`: id, tenant_id, plan_id, status ENUM('trial','active',
     'expired','cancelled'), trial_ends_at, current_period_end, created_at.
2. Seed `plans` with two rows: 'free' (current default) and 'pro' (price TBD, 
   set a placeholder value we can edit later).
3. New tenants get a tenant_subscriptions row with plan_id='free', status='active' 
   automatically at signup.
4. Add GET /api/tenant/subscription — returns the tenant's current plan/status.
5. Replace every existing hardcoded `tenant.plan === 'pro'` check (in the code 
   editor route gate, and anywhere else it appears) with a real check against 
   tenant_subscriptions: status is 'active' or 'trial' AND plan_id points to the 
   'pro' plan.


## Fix B: Upgrade flow (currently the "Upgrade Plan" button just console.logs)


1. Add POST /api/tenant/subscription/upgrade — for now, since real payment gateway 
   integration is a separate task, this should:
   - Create/update the tenant's tenant_subscriptions row to plan_id='pro', 
     status='trial', trial_ends_at = now + 14 days.
   - This mirrors the same "stub now, real payment later" pattern already used for 
     Telebirr/Chapa booking payments — don't build real billing yet, just make the 
     plan change itself functional and real.
2. Wire the Settings page "Upgrade Plan" button to call this endpoint instead of 
   console.log. On success, show a toast and update the visible plan status in 
   Settings and any plan-gated UI (e.g. the code editor becomes accessible without 
   a page reload if possible, or at minimum after a refresh).
3. Add a simple "Plan & Billing" section in Settings showing: current plan, status, 
   trial_ends_at if applicable. Leave actual recurring billing/payment collection 
   explicitly out of scope — label this section honestly, e.g. a note that real 
   billing integration is coming.


## Fix C: Storage limits on Pro editor files


1. Add a per-tenant storage cap check in PUT /api/tenant/pro-site/files: sum the 
   byte length of all files being saved for this tenant, reject with a 413 error 
   and clear message if it exceeds a configurable limit (start with 10MB total per 
   tenant — reasonable for a small React project, adjust later if needed).
2. Also cap individual file size (e.g. 1MB per file) to prevent one giant pasted 
   blob from silently consuming the whole quota.
3. Surface this limit clearly in the code editor UI — if a save is rejected for 
   size, show the actual error to the user (e.g. "Your project exceeds the 10MB 
   storage limit"), not a silent failure.


Verify: 
- A free-tier tenant cannot access /dashboard/code-editor (redirected or blocked).
- Clicking "Upgrade Plan" in Settings actually changes the tenant's plan to Pro 
  with a 14-day trial, and the code editor becomes accessible.
- Attempting to save files exceeding the size cap shows a clear error instead of 
  succeeding or failing silently.
'''
prompt 1-3 
'''
You are working on the Egebeya project at D:\Workstation\egebeya. Fix two structural risks: payment webhook forgery/replay, and unenforced multi-tenant 
query isolation. Do not skip verification steps.


## Part A: Payment webhook security
use test mode and space for live credentials if the live isn't found it will fall back to test mode 


1. Check the current /api/payments/webhook handler. Look up the ACTUAL current 
   webhook verification mechanism documented by Chapa and Telebirr (their real 
   docs — do not guess field names). Most payment providers sign webhook payloads 
   with a secret and expect you to verify a signature/hash before trusting the 
   payload.
2. Implement signature verification for both providers before processing any 
   webhook payload. Reject (401/403) any webhook whose signature doesn't match — 
   log the rejection, don't silently ignore it.
3. Implement idempotency: store the provider's transaction/reference ID on 
   successful processing (add a column or table if one doesn't exist — e.g. 
   `processed_webhook_events`). Before acting on any webhook, check if that 
   transaction ID was already processed; if so, return success without 
   re-processing (this handles provider retry behavior safely).
4. Write a test simulating: (a) a forged webhook with no/invalid signature — 
   confirm it's rejected, (b) the same valid webhook delivered twice — confirm 
   only one booking/payment state change happens.


## Part B: Multi-tenant query enforcement


1. Audit every route/model file that queries tenant-scoped tables (bookings, 
   services, staff, media, pro_site_files, pages, tenant_business_hours, etc.) 
   for missing tenant_id filters. List every instance found, even ones that look 
   fine — I want a full inventory, not just fixes.
2. Build a shared data-access layer that makes tenant scoping structurally 
   required rather than optional — e.g. a helper like 
   `db.forTenant(tenantId).query(...)` or per-model repository functions that 
   require tenantId as a mandatory first argument, so a future query literally 
   cannot compile/run without it.
3. Migrate all tenant-scoped queries found in step 1 to use this new layer.
4. Add a permanent automated test (not a one-off manual check) that: creates two 
   tenants, creates data for each, authenticates as Tenant A, and attempts to 
   fetch every tenant-scoped resource type using Tenant B's IDs — asserting every 
   attempt is rejected. This test should run in CI going forward (see the 
   process-risk prompt), not just once now.


Report: the full inventory from step 1, what was fixed, and confirm the new 
isolation test passes.
'''
prompt 1-4
'''
You are working on the Egebeya project at D:\Workstation\egebeya. Establish 
automated regression testing and living architecture documentation.

1. Expand the test suite (building on the existing booking-concurrency test and 
   the new tenant-isolation/webhook tests from the structural-fixes pass) to 
   cover: auth (register/login/refresh), core booking CRUD, plan-gate enforcement 
   (Free tier blocked from Pro routes), and the payment webhook signature/
   idempotency behavior.
2. Set up GitHub Actions (or note if a different CI is already in use) to run 
   the full test suite automatically on every push, and block merging/deploying 
   if tests fail.
3. Generate an ARCHITECTURE.md at the repo root documenting: current tech stack, 
   database schema (tables and relationships), the tenant-resolution/routing 
   logic (subdomain → Puck vs Pro-tier serving), the auth flow (access + refresh 
   token), the payment webhook flow, and the Pro-tier editor/publish pipeline as 
   it currently exists. This should reflect the ACTUAL current code, not the 
   original plan — verify against real files, don't reconstruct from memory of 
   past instructions.
4. Add a short CONTRIBUTING.md or dev-setup section noting how to run tests 
   locally and how to deploy to staging vs production.

Report: test coverage summary, CI status, and confirm ARCHITECTURE.md accurately 
reflects the current codebase (spot-check a few claims in it against actual files).
'''
prompt 1
'''
You are working on the Egebeya project at D:\Workstation\egebeya. In the Pro-tier 
code editor (/dashboard/code-editor), add protection against losing unsaved work.

1. Track a `hasUnsavedChanges` state: set to true whenever Monaco content changes 
   from what was last successfully saved, false immediately after a successful "
   Save API call.
2. Add a `beforeunload` event listener that triggers the browser's native "leave 
   site?" confirmation dialog if hasUnsavedChanges is true when the user tries to 
   close the tab or navigate away. Remove the listener on unmount/after save to 
   avoid it firing unnecessarily.
3. Add autosave: debounce Monaco content changes (e.g. 5-10 seconds after the user 
   stops typing) and automatically call the same Save endpoint used by the manual 
   Save button. Show a small, unobtrusive "Saved" / "Saving..." indicator near the 
   Save button rather than a toast for every autosave (toasts on every autosave 
   would be noisy).
4. If autosave fails (network error, size limit exceeded from the earlier storage 
   cap fix), do NOT silently reset hasUnsavedChanges to false — keep the 
   unsaved-changes warning active and surface the failure clearly.

Verify:
- Make an edit, wait ~10 seconds without clicking Save, confirm it autosaves 
  (check network tab or reload to confirm persistence).
- Make an edit, immediately try to close the tab, confirm the browser shows a 
  leave-confirmation prompt.
- After a successful save (manual or auto), confirm the leave-confirmation no 
  longer triggers if you try to close immediately after.
'''
prompt 2
'''
You are working on the Egebeya project at D:\Workstation\egebeya. Establish 
automated regression testing and living architecture documentation.

1. Expand the test suite (building on the existing booking-concurrency test and 
   the new tenant-isolation/webhook tests from the structural-fixes pass) to 
   cover: auth (register/login/refresh), core booking CRUD, plan-gate enforcement 
   (Free tier blocked from Pro routes), and the payment webhook signature/
   idempotency behavior.
2. Set up GitHub Actions (or note if a different CI is already in use) to run 
   the full test suite automatically on every push, and block merging/deploying 
   if tests fail.
3. Generate an ARCHITECTURE.md at the repo root documenting: current tech stack, 
   database schema (tables and relationships), the tenant-resolution/routing 
   logic (subdomain → Puck vs Pro-tier serving), the auth flow (access + refresh 
   token), the payment webhook flow, and the Pro-tier editor/publish pipeline as 
   it currently exists. This should reflect the ACTUAL current code, not the 
   original plan — verify against real files, don't reconstruct from memory of 
   past instructions.
4. Add a short CONTRIBUTING.md or dev-setup section noting how to run tests 
   locally and how to deploy to staging vs production.

Report: test coverage summary, CI status, and confirm ARCHITECTURE.md accurately 
reflects the current codebase (spot-check a few claims in it against actual files).
'''
prompt 3
'''
You are working on the Egebeya project at D:\Workstation\egebeya. Run a 10-point 
security self-audit, scoring each item honestly, then fix whatever is missing or 
incomplete. Report the score before and after fixes, like a checklist — don't 
just say "done," show the actual verification for each.

1. Rate limiting: inventory every public-facing endpoint (auth, public booking, 
   public availability, media upload, payment webhook). Confirm rate limiting is 
   applied to each, not just auth. Add it wherever missing, tuned appropriately 
   (e.g. stricter on auth/OTP, looser on read-only public endpoints).

2. Secrets server-side only: search the full git history and the production 
   frontend build output for any API key, DB credential, or JWT secret. Confirm 
   .env is and always was in .gitignore. Report anything found as a flagged 
   finding requiring rotation, not a silent fix.

3. Tenant data isolation (our MySQL equivalent of row-level security, since MySQL 
   has no native RLS): confirm every tenant-scoped table (bookings, services, 
   staff, media, pro_site_files, pages, business_hours) is only ever accessed 
   through the shared tenant-scoped data-access layer built in the earlier 
   structural fix — not any leftover raw query bypassing it. Run the existing 
   cross-tenant isolation test and confirm it still passes.

4. .env hygiene: same as item 2, confirmed.

5. Input validation/sanitization: re-run injection tests (SQL injection strings, 
   XSS payloads) against every text input across the app — booking fields, 
   service/staff names, Settings fields, Puck editor content, Pro-tier Sandpack 
   file contents. Confirm sanitization on both save and render, especially on 
   PUBLIC-facing rendering of tenant-authored content (a stranger viewing a 
   tenant's page should never be exposed to unescaped injected content).

6. No endpoint returns tenant data without both auth AND tenant-id checks: 
   inventory every API route. For each, classify as (a) intentionally public 
   (discover listing, public booking), (b) requires auth only, or (c) requires 
   auth + tenant match. Confirm every route in category (c) actually enforces 
   both — this is the same check as item 3 but from the routing side rather than 
   the data-access side, to catch anything the data-access layer migration might 
   have missed.

7. Auth on protected routes: full route inventory (frontend AND backend) — list 
   every route, whether it requires auth, and confirm the middleware is actually 
   applied, not just assumed present. Include the newer /admin superadmin panel 
   and Pro-tier code editor routes explicitly, since those were added after 
   earlier audits.

8. No stack traces or internal details leaked to users: trigger deliberate server 
   errors (malformed requests, missing required fields, DB constraint violations) 
   and confirm API responses return clean, generic error messages — never raw 
   stack traces, file paths, or query text.

9. Admin/debug endpoints locked down: confirm /admin requires is_superadmin, not 
   just being logged in. Search the codebase for any leftover test/debug/seed 
   endpoints from earlier development sessions (e.g. anything like /api/test, 
   /api/debug, or seed-triggering routes) that might still be reachable in the 
   current build — remove or properly gate any found.

10. Security event logging (distinct from Sentry's error tracking): add a 
    dedicated security log (a table or structured log file) recording: failed 
    login attempts, rejected payment webhook signatures, rate-limit triggers, 
    and any cross-tenant access attempt caught by the isolation checks. This 
    should be queryable/reviewable by you later — even a simple 
    security_events table (event_type, tenant_id, ip, details, created_at) is 
    enough for now; a dashboard for it can come later.

Final report format: for each of the 10 items, state PASS / FIXED / STILL AT RISK, 
with a one-line justification citing what you actually checked — not just an 
assertion. Give an honest score out of 10 at the end.
'''
prompt for Hermes 
'''
Prompt for Hermes — Full Platform QA Audit (Pro-User Simulation)

You are acting as a skeptical, technically sophisticated Pro user auditing the Egebeya 
platform at [your local/staging URL]. Your job is not to confirm things work — it's 
to actively try to break them, find what's inconsistent, and flag anything that would 
embarrass the product in front of a real paying customer. Be thorough over fast. 
Where something worked in a prior audit, re-verify it — don't assume it still does.

Do NOT fix anything during this pass. This is audit-only. Produce a report.

═══════════════════════════════════════
PART 1: FULL SITE CRAWL — EVERY ROUTE, EVERY ROLE
═══════════════════════════════════════

Test as each of these distinct users, and note which role you were acting as for 
every finding:

1. Anonymous visitor (never logged in)
2. Newly registered owner, mid-onboarding (before completing Setup Wizard)
3. Fully onboarded owner (Free/Standard tier)
4. Owner upgraded to Pro tier
5. Staff-role user (not owner)
6. A second, unrelated tenant (to test cross-tenant data isolation — see Part 4)

For each role, visit and interact with EVERY reachable route, not just the obvious 
ones. Build a list of every route from App.tsx / router config first, then work 
through it systematically:

- Landing page, all its sections and links (nav, footer, pricing, CTA buttons)
- Register, Login, Forgot Password, Reset Password
- Setup Wizard — all steps, including going back/forward, refreshing mid-wizard, 
  and abandoning it partway to see what state that leaves the account in
- Dashboard Overview, Bookings, Services, Staff, Media Library, Settings, Website 
  Editor (Puck), Billing/Plan section, Pro Code Editor (Sandpack)
- Discover page and every business card's outbound link
- Public tenant site (Puck-rendered) — every block type
- Public booking flow — full multi-step booking as a customer, including 
  cancelling, rescheduling if available, and the confirmation state
- 404 page, and any other error states you can trigger
- Any route you find that's linked from a component but not obviously part of the 
  main navigation (orphaned/forgotten pages are common — look for them)

For each page/route, explicitly test:
- Does every button, link, and form actually do what its label says?
- Are there any console errors or failed network requests (check DevTools)?
- Does the page handle empty states gracefully (zero bookings, zero services, 
  zero staff, zero media)?
- Does it handle loading states (slow network simulation) without flashing broken 
  content or layout shift?
- What happens on refresh mid-flow (wizard, booking, editor)?
- What happens on browser back button mid-flow?

═══════════════════════════════════════
PART 2: DESIGN CONSISTENCY AUDIT
═══════════════════════════════════════

The platform has a documented design token set (post-Impeccable unification: brand 
colors, spacing, typography). For every page visited in Part 1, check:

- Are colors pulled from the actual token set, or are there raw hex values, 
  arbitrary Tailwind values (bg-[#xxxxxx]), or leftover custom CSS variables from 
  before unification (--color-telebirr, --color-paper, --color-ink, etc.)?
- Is typography consistent (font family, weight, sizing scale) across Landing, 
  Dashboard, Puck-rendered public pages, and the Sandpack Pro editor chrome?
- Do buttons, cards, form inputs, and modals look and behave the same way across 
  different sections, or does each section seem to have been built independently 
  with its own component styling?
- Specifically check the Sandpack Pro editor UI — does its chrome (toolbar, file 
  tree, panels) match the rest of the dashboard, or does it look like an 
  unstyled third-party embed?
- Specifically check the Puck editor's default styling — same question.
- Spacing/alignment: any pages where padding/margins look visibly inconsistent 
  with neighboring pages of the same type (e.g. two dashboard pages with 
  different card padding)?
- Report every inconsistency with: which page, which specific element, what it 
  should match, and a screenshot description or exact CSS class/file reference.

Do not just say "design is inconsistent" — name the specific surfaces and the 
specific token/class mismatch.

═══════════════════════════════════════
PART 3: AI SLOP / FAKE CONTENT AUDIT
═══════════════════════════════════════

Go through every page looking for content that would make a real user distrust 
the product:

- Hardcoded fake data presented as if real (ratings, locations, testimonials, 
  reviews, stats) — flag exact file and line
- Stock/generic imagery (Unsplash placeholders, generic icon sets) used where 
  real tenant-uploaded content should appear
- Placeholder/lorem-ipsum-style copy left in production paths (not intentional 
  input placeholders — actual displayed content)
- Any UI element implying a feature exists that doesn't actually work (a button 
  that looks fully functional but silently does nothing, vs. one honestly labeled 
  "Coming soon")
- Generic, unbranded error messages ("Something went wrong") where a specific, 
  helpful message should appear
- Any remaining alert()/console.log-only feedback instead of real UI feedback

═══════════════════════════════════════
PART 4: SECURITY AUDIT
═══════════════════════════════════════

Test each of these directly — don't just inspect code, actually attempt them:

**Multi-tenant data isolation (highest priority — this is a multi-tenant SaaS, 
a leak here is catastrophic):**
- Log in as Tenant A. Note IDs of Tenant A's bookings, services, staff, media, 
  pro_site_files. Log in as Tenant B. Attempt to access Tenant A's resources 
  directly via API calls with Tenant A's IDs (e.g. GET /api/bookings/{tenantA's 
  booking id}, GET /api/tenant/pro-site/files while authenticated as B). Every 
  one of these should be rejected. Report any that succeed — this is a critical 
  finding if found.

**Authorization vs. UI-only restriction:**
- Log in as a staff-role user. The UI hides certain sidebar items — but directly 
  call the underlying API endpoints for hidden sections (Services delete, Staff 
  delete, Settings update, Publish) using the staff user's token. Confirm the 
  backend rejects these, not just the frontend. A hidden button is not a security 
  control — only a backend check is.

**Plan-gate bypass:**
- As a Free/Standard tier tenant, directly call the Pro-tier endpoints 
  (/api/tenant/pro-site/*) with your Free-tier token. Confirm they're rejected 
  server-side, not just hidden from navigation.

**Injection & input validation:**
- Try SQL-injection-style strings and XSS payloads (e.g. `<script>alert(1)</script>`, 
  `' OR '1'='1`) in every text input across the app — booking customer name/notes, 
  service names, business name, Settings fields, Puck editor text blocks, Sandpack 
  file contents. Confirm inputs are sanitized/escaped on both save and render (in 
  the DASHBOARD and in the PUBLIC-facing rendering — public pages showing 
  unescaped tenant input is a real risk since strangers can be attacked via a 
  tenant's page).

**File upload risk (Media Library):**
- Attempt to upload a non-image file disguised with an image extension, an 
  oversized file, and a file with a script payload in metadata. Confirm the 
  backend validates actual file content/type, not just the extension, and 
  enforces a size limit.

**Auth & session:**
- Confirm rate limiting actually triggers on repeated failed login/OTP attempts 
  (brute-force protection) — try it, don't just check code exists.
- Confirm the refresh token flow can't be replayed after logout (log out, then 
  try using the old refresh token — should fail).
- Check what's in localStorage after login — is anything more sensitive than 
  necessary being stored client-side in plain text?

**Secrets exposure:**
- Search the built frontend bundle (production build output) for any API keys, 
  DB credentials, or JWT secrets that may have leaked into client-side code. 
  Anthropic/E2B/StackBlitz keys especially — confirm none of these ever appear 
  in anything shipped to the browser.

**HTTP security headers:**
- Check response headers on both the API and the served frontend for: 
  Content-Security-Policy, X-Content-Type-Options, X-Frame-Options (or 
  frame-ancestors), Strict-Transport-Security. Report which are missing.
- Confirm the COEP/COOP headers (if still present from earlier WebContainer work, 
  or absent if migrated to Sandpack) are scoped only to the routes that need them 
  and aren't breaking anything else or left over unnecessarily.

**Error handling / information leakage:**
- Trigger server errors deliberately (malformed requests, missing fields) and 
  confirm the API never returns raw stack traces or internal file paths to the 
  client — only clean error messages.

═══════════════════════════════════════
PART 5: OTHER ANGLES OFTEN MISSED
═══════════════════════════════════════

**Accessibility:**
- Keyboard-only navigation: can you complete the booking flow and dashboard CRUD 
  operations using only Tab/Enter, no mouse?
- Are images missing alt text? Are form inputs missing associated labels? 
  Check with a screen reader if available, or at minimum inspect the DOM for 
  aria attributes on interactive elements.
- Check color contrast on key text against the brand palette (especially amber 
  accent on light backgrounds — this combination often fails contrast checks).

**Performance:**
- Note any pages with visibly slow load (large unoptimized images, huge JS 
  bundles). Run a Lighthouse pass if tooling allows and report the scores.
- Check whether uploaded media is actually resized/compressed or served at full 
  original resolution.

**Mobile & responsive:**
- Test the full booking flow, dashboard, and Puck/Sandpack editors at a mobile 
  viewport width. Note anything unusable (the code editor in particular is likely 
  to break on mobile — confirm whether it's gracefully restricted to desktop or 
  just silently broken).

**SEO (for public tenant pages specifically):**
- Check whether public tenant pages have unique <title> and meta description per 
  tenant, or whether they all share the same generic SPA shell title.
- Check for a robots.txt and whether it's sensibly configured (dashboard/API 
  routes should not be indexable; public tenant/discover pages should be).

**Internationalization readiness:**
- Confirm whether any Amharic text/locale switching exists yet, or whether the 
  app is English-only. Just report current state — not expected to be complete.

**Data integrity / business logic edge cases:**
- Attempt to book two overlapping appointments for the same staff member 
  simultaneously (open two tabs, submit both quickly) — confirm only one succeeds.
- Attempt a booking on a day marked closed in business hours, and a booking in 
  the past, via direct API call bypassing the UI's date picker restrictions.
- Check Ethiopian vs. Gregorian calendar display — confirm date/time actually 
  shown to a customer matches what gets stored and what the business owner sees 
  in their dashboard (a mismatch here would cause real missed appointments).
- Check currency formatting consistency (ETB) across booking, services pricing, 
  and any billing/plan pricing display — same format everywhere?

**Compliance-adjacent technical gaps (flag only — do not draft legal text, that's 
being handled separately):**
- Is there any consent capture at signup or booking (checkbox, notice) before 
  collecting phone numbers/personal data?
- Is there any user-facing way to request data deletion or export, even a stub?
- Flag these as findings only — no action needed from you beyond listing them.

**Storage/quota/limits:**
- Confirm the earlier Pro-tier file size caps actually reject oversized 
  saves with a clear error, not a silent failure or server crash.

**Stubbed features — verify they're honest, not broken-looking:**
- Publish button (Pro editor), Upgrade Plan flow, AI Assistant panel, Custom 
  domain field — confirm each clearly communicates its current state rather than 
  appearing to silently fail.

═══════════════════════════════════════
REPORTING FORMAT
═══════════════════════════════════════

Structure your final report exactly like this:

1. Executive Summary (2-3 sentences, honest overall state)
2. Critical Findings (security/data-isolation issues — anything here should stop 
   a launch)
3. Functional Issues & Broken Workflows (severity: High/Medium/Low, steps to 
   reproduce, exact file references)
4. Design Inconsistencies (page/element, issue, evidence — file/line if possible)
5. AI Slop / Fake Content (exact location, what should replace it)
6. Security Findings (organized by the categories in Part 4, severity-ranked)
7. Accessibility, Performance, SEO, Mobile findings
8. What Was Fully Verified Working (don't just report problems — list what 
   passed, so it's clear what's actually solid)
9. Readiness Score table by category, same format as the original QA audit, 
   plus a comparison note: what's improved since the last audit, what regressed 
   if anything, what's newly untested territory (Pro tier, billing, etc.)
10. Prioritized fix list — ordered by risk, not by ease

Be specific everywhere. "Design is inconsistent" is not a finding. "The Sandpack 
editor toolbar uses default gray (#e5e7eb) instead of the brand surface token, 
visible at src/pages/Dashboard/CodeEditor.tsx line ~140" is a finding.
'''
