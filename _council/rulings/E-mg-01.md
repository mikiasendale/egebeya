ID: E-mg-01 (+ Fresha pair F-sa-130)
Name: Blast campaigns (email & text) — scheduling, builder, performance view
Surface: merchant SaaS (A/C/D declared; B's paper-wide gap noted — no clash)
Category: marketing-growth
Class: PARTIAL (COMPARE-01:135, :331)
Fresha behaviour: Bulk email + SMS campaigns with a builder, scheduling, audience targeting, resend, and per-campaign performance. (OBSERVED — hc/101311,101312,101314,101315,103794,103796,101368,101369.)
Egebeya current state: One-shot SMS blast with STOP suffix exists (src/api/crm.ts:182-244) as a synchronous in-request loop — every recipient awaited, no throttle, no resume, no campaign identity; notification_log already carries a template column (schema.ts:438-449). Email is a logged stub without SMTP_HOST (mailer.ts:31-34 records stub sends as success).
Blockers: infra (SMTP not provisioned — email campaigns blocked, "do not sell a stub"); sequencing (E-ca-02 consent duty ships FIRST); money-adjacent (register lists E-mg-01; campaign-attached codes).
Debate summary: B's tree fact settled the row's central dispute: Gap.md describes a "Pro gate" on /marketing/blast but no plan gate exists in code (crm.ts:182 — owner+CSRF+limiter only); tree wins (rule 17), and the Chair recorded Gap.md's claim as stale against C/D's OBSERVED citations. B priced M with a marker-first fix (campaigns + campaign_recipients riding the billing_reminder_sends precedent, schema.ts:405-415) — "the only one of the 68 that genuinely wants worker semantics; at 25 tenants marker-first in-process is sufficient." A sequenced it strictly after E-ca-02: "expanding marketing sends before consumers can withdraw consent is backwards." C added the conditions: stub-stats fix before any email perf claim, never-throw, >10k throttle stays parked. D: blasts convert trust into complaints until STOP is honored; incumbent workaround is Telegram broadcast lists.
Council ruling: BUILD NEXT — Band 2, strictly after E-ca-02 (Chair): scheduling + per-template performance view, SMS-only. Email campaigns: blocked until SMTP is provisioned.
Closure method (if BUILD): Scope = campaign identity + scheduled sends + per-template performance view from notification_log. Entity changes: additive `campaigns` + `campaign_recipients` marker rows (billing_reminder_sends precedent; guarded idempotent ADD per AGENTS.md), inline eq(tenantId). Endpoint changes: campaign CRUD + schedule on /marketing routes under existing owner+CSRF+limiter; send loop becomes bounded in-process chunks (no Redis), resume-safe via markers — or rides ruling-12's job_outbox from day one: the enqueue-shape choice is the Engineering Lead's to propose at closure (CEO §Impacts r12 bullet). UI changes: schedule picker + per-template delivery view, both locales. Test additions: money-gate cases (B/C) — blast resume without double-send; campaign-attached code is single-use and consent-gated; mailer stub-truth bug (mailer.ts:31-34) fixed before any email performance claim; channels never throw (AGENTS.md:22).
Substitute method (if any): none — the blast ships today; the missing halves are opt-out (E-ca-02) and campaign identity, both ruled as builds, not substitutes.
Owner: Engineering Lead
Effort: M (B)
Money path: YES ⇒ CAUTION (money-register: campaign-attached discounts ride the promo path; the SMS itself carries no money — B: "no money in SMS" — but any attached code must pass the single-use/consent gate case)
Protected decision referenced (if any): none reopened; EGE-ADV #2 (unmetered messaging) is the collision tripwire — metering email to merchants would reverse it (see collision line).
EGE-ADVANTAGE collision (if any): YES-if-email-metered — C: email introduces the first per-send platform cost against #2/#12; "merchant-SMTP / absorb / meter" is a CEO posture ask (C ask 7), only live if email campaigns are pursued. The build as ruled (SMS-only) collides with nothing.
Merchant evidence: OBSERVED-workaround (Telegram broadcast lists — free, Amharic-capable, already installed; D); day-1 y (blast already ships; the missing half is opt-out); A's MED impact.
Confidence: MEDIUM on the full row (email scope, Pro-gate, cost posture all parked); HIGH on the ruled SMS-only Band-2 shape
CEO ruling (final): APPROVED at CP5. Residual answered: **blast IS to be Pro-gated** — add the plan gate to `POST /api/tenant/marketing/blast` (crm.ts:182; tree currently has none — Gap.md's "Pro gate" claim becomes true of intent, fixed in code), one acceptance case: free-plan tenant → 403 PLAN_REQUIRED. Email-metering posture (EGE-ADV #2 reversal) remains parked until email is pursued. See ceo-rulings-cp4.md CP5 ADDENDUM item 1.
