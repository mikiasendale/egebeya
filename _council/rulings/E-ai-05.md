ID: E-ai-05 (+ Fresha pair F-sa-264)
Name: Data export / BI connector
Surface: merchant SaaS
Category: api-integrations (INFRA-BLOCKED grouping in Gap.md)
Class: RECLASSIFIED per CEO ruling 12: INFRA-BLOCKED → SUBSTITUTE (the continuous/streaming connector stays parked; the scheduled-export substitute is the ruling shape)
Fresha behaviour: Continuous export of bookings, clients and payments to an external BI or spreadsheet system (Data Connector add-on). KB: COMPARE-01:201 (/pricing add-on row), docs/Gap.md:355-358.
Egebeya current state: One-shot streaming CSV endpoint exists with ZERO UI callers — GET /api/tenant/export/csv (src/api/tenant.ts:2122+, verified); no scheduled export, no object sink.
Blockers: infra for the real connector only (continuous streaming "genuinely wants a worker"; B) — parked behind ruling 12 Phase 2/3, not blocking the substitute; Spec B Phase 0 (cron_locks) is a precondition for the new export cron to be safe
Debate summary: A offered two substitutes — NOW: wire the live CSV button (T7.17); NEXT: a nightly snapshot on the existing cron + media-storage pattern, "no queue needed at 25 tenants," while refusing to sell a connector the single process can't carry. B: S substitute / L real; the stack-fitting shape is an 8th cron weekly export (isDirectRun precedent, settlementReconciliation) writing CSV + emailing a link; "build the lease and the outbox, not the Redis." C: AMBER substitute — pull-on-demand only, UI + regenerate + security_events export logging; named trade-off: no scheduled push, no object sink; a streaming ETL inside the request handler would regress the <500ms web principle (Gap.md:375). D: impact MED — the merchant's sentence is never "BI connector," it's "give me my bookings in a file I can open"; ship the button AND a scheduled emailed CSV. Chair logged: INFRA-BLOCKED covers only the continuous half; the button is unanimous; snapshot cadence (nightly A / weekly+email B / monthly D / pull-only C) contested and escalated with the queue funding question. CEO ruling 12 resolved it: weekly cron, per-tenant per-location CSV to storage, Reports download + existing pull endpoint; reclassifies the row to SUBSTITUTE.
Council ruling: SUBSTITUTE (BUILD NEXT on the substitute shape; continuous streaming ETL stays INFRA-BLOCKED/parked)
Closure method (if BUILD): scope: (1) wire the existing one-shot CSV to a Reports-page download button (dead-surface resurrection, T7.17 — C's regenerate + security_events export audit); (2) weekly cron writes per-tenant, per-location CSV snapshots to the existing media storage; Reports page lists/downloads snapshots; same CSV on demand via the existing export endpoint = pull-only. Entity changes: none required for pull; snapshot rows (export_files table or storage-path convention) additive, guarded plain ADD COLUMN; snapshot schema carries a location dimension so ruling 10 needs no re-shape (tenant==location today; location_id reserved). Endpoint changes: none to tenant.ts:2122 semantics; new tenant-scoped GET listing snapshot files. UI changes: Reports page Download + snapshot list; am/en strings. Test additions (Spec B list, verbatim per ruling 12): two instances one cron → one execution (the export cron acquires a Phase 0 `cron_locks` lease — markers before locks); same job twice → one effect; cross-tenant leak impossible — eq(tenantId) on every export query (the inline-eq law; this row's queries included); weekly export ≡ pull endpoint output (byte-compare for the same window); poller/restart keeps unprocessed work (applies once Phase 1 `job_outbox` exists — export job rides it then, not before). Cron touches: adds one (the 8th — hence the lease precondition). Spec B citation: Phase 0 (cron_locks) precedes the new cron; Phase 1 (job_outbox) is the migration path for scheduled dispatch; Phase 2 (Postgres decision this quarter) gates any future streaming/continuous shape; ruling 10 makes export-per-location load-bearing in the snapshot schema.
Substitute method (if any): THE ruling — weekly cron → per-tenant per-location CSV in storage + Reports download + existing pull endpoint. Named trade-off: data is up to 7 days stale between pulls (no streaming sink); continuous ETL stays parked pending the Postgres decision this quarter (ruling 12 Phase 2).
Owner: Engineering Lead (cron + lease + tests) / Product Owner (Reports surface)
Effort: M
Money path: NO (payments CSV is a read-only export of existing records; moves nothing — but its queries fall under the eq(tenantId) audit)
Protected decision referenced (if any): ruling 12 Spec B (cron-lease + job_outbox; "What stays parked: continuous ETL, BullMQ/Redis, two-way sync"); ruling 10 (multi-location strategy — export-per-location shape); inline eq(tenantId) law (AGENTS.md); Gap.md:375 <500ms web principle (no streaming ETL in-request).
EGE-ADVANTAGE collision (if any): NO (C §3) — pull-on-demand serves the actual job at current scale without copying Fresha's connector posture.
Merchant evidence: OBSERVED-workaround frequency (endpoint without button, code-proven dead surface); workaround = ask the platform for a file; merchant sentence per D: "give me my bookings in a file I can open"; notice day 1: n — y at first reconciliation / first threat of leaving.
Confidence: HIGH (substitute shape now CEO-fixed; cadence debate superseded by ruling 12)
CEO ruling (final): Ruling 12 — RECLASSIFY INFRA-BLOCKED → SUBSTITUTE: weekly cron writes per-tenant per-location CSV to storage + Reports page download + existing pull endpoint; trade-off: up to 7 days stale; continuous streaming ETL stays parked pending the Postgres decision this quarter; Effort M.
