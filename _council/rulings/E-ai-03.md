ID: E-ai-03 (+ Fresha pair F-sa-260)
Name: Calendar sync — CEO-redefined to shared business calendar (external halves stay parked)
Surface: merchant SaaS
Category: api-integrations (INFRA-BLOCKED grouping in Gap.md)
Class: INFRA-BLOCKED (original F-sa-260) → TWO RULINGS IN ONE: internal half de-classified to ordinary product work (BUILD NEXT); external half stays INFRA-BLOCKED with a named SUBSTITUTE (ruling 12, "What stays parked")
Fresha behaviour: Two-way sync between Fresha and Google/Apple/ICS calendars so external bookings don't double-book the chair; staff see their schedule. KB: COMPARE-01:198 (hc/101373), docs/Gap.md:350-354.
Egebeya current state: One-way publish only — the read-only public appointments feed (src/api/public.ts:1432-1478, verified: currently JSON; ICS serialization + per-staff scoping is the new S-work at that seam); no shared staff/owner calendar view; no inbound sync plumbing.
Blockers: internal half: none. External half: infra (durable inbound retry + per-provider OAuth) — parked behind ruling 12's Phase 3, not built around
Debate summary: The CEO redefinition (docs/Gap.md:354) split the row into two lines: an internal shared staff/owner calendar (a UI build, not a sync build) and the true two-way external sync. A: the internal grid is "the single largest small diff in the session," zero infra, NEXT; external two-way stays behind the queue. B de-classified it — internal = read of appointments grouped by staff over the E-ms-01 grid + owner-schedules-all as a role tweak on existing gates (bookings.ts:23-41); external remains blocked; substitute = keep the one-way feed, add per-staff variants (S). C re-scope GREEN but forbidden a second calendar truth: must derive from appointment columns (AGENTS:21-22) and "must not promise sync it can't do." D: "the single most ask-shaped line in the Gap file" — every owner on a second chair is a human message bus ("ስለዚህ ቀን አለህ?" on Telegram weekly; the E-ms-02 workaround, Gap.md:184). The queue/lease shapes (A's Phase 0-lite vs B's cron_leases+job_outbox vs C's six floors) were escalated; CEO ruling 12 settled the execution model (cron-lease + job_outbox, Option 1 shape) and parked two-way sync.
Council ruling: BUILD NEXT (internal shared calendar; two rulings in one file); external two-way sync: INFRA-BLOCKED — stays PARKED per ruling 12, shipped via its substitute
Closure method (if BUILD) — internal half: scope: the shared staff/owner calendar as the E-ms-01/E-ms-02 bundle — read-only week grid (v1: rendered grid, no drag, no color-config, per Chair scope law), staff login sees their own week via existing role projection (bookings.ts:23-58), owner sees/schedules all. Entity changes: none — derived from appointment columns (AGENTS.md:21-22; no parallel schedule table, no second truth). Endpoint changes: none new; scoped reads over GET /api/bookings. UI changes: new calendar view beside the queue stack (server/lib/queue.ts untouched); unlocks the E-bc-08 merchant-half reschedule inside the bundle and makes E-ms-03 closures visible. Test additions: staff sees only own appointments; owner sees all; grid agrees with queue/day-list derivations; copy never claims "sync." Effort split: the grid bundle is M (B); the row rides E-ms-01's component landing once.
Closure method (if BUILD) — external substitute: scope: one-way per-staff ICS subscription URLs riding the existing feed seam (public.ts:1432-1478) — staff-scoped filter + ICS serialization, secret token in URL, revocable; S per B. Entity changes: none (or a per-staff feed-token column, guarded ADD COLUMN). Endpoint changes: public GET feed gains ?staff/ token scoping (inline eq(tenantId) law). Test additions: token revocation; cross-tenant/cross-staff leak impossible; feed content = confirmed+pending only (as today).
Substitute method (if any): the per-staff one-way ICS subscription feed (above). Named trade-off: external calendars never write back — double-booking the chair against external events stays unprevented until the parked two-way sync earns its Phase 3 build.
Owner: Product Owner (internal bundle) / Engineering Lead (ICS substitute)
Effort: M (internal bundle; substitute S)
Money path: NO
Protected decision referenced (if any): ruling 12 "What stays parked" (two-way sync, continuous ETL, BullMQ/Redis); ruling 12 Spec B (queue funding resolved Option 1 — lease + outbox, Postgres decision this quarter); AGENTS.md:21-22 (queue derives from appointment columns; do not refactor); Chair scope law on E-ms-01 v1; E-ms-02 read-scoping form (C's, not B's dated shifts).
EGE-ADVANTAGE collision (if any): collision risk only if the grid forks a second calendar truth (C §3: "YES if it forks a second calendar truth") — derivation from appointment columns is the guard. Also EGE-ADV (Ge'ez scheduling): feed copy keeps Ethiopian-calendar display honesty per existing behavior.
Merchant evidence: DECISION-recorded frequency (CEO re-scope) + the raw ask UNDOCUMENTED (Season 0); workaround = owner as human message bus between calendar and staff, phone calls to ask "am I on today?"; notice day 1: y.
Confidence: HIGH (internal — all four concordant); HIGH on the parked classification (CEO-locked), MEDIUM on substitute depth
CEO ruling (final): Per ruling 12 + docs/Gap.md:354 redefinition — internal shared staff/owner calendar = the E-ms-01/02 bundle, BUILD NEXT, no infra dependency (D: the top ask-shaped row); external half stays PARKED ("What stays parked"); substitute = one-way per-staff ICS subscription URLs riding the existing feed (public.ts:1432-1478); trade-off: external calendars never write back, double-booking vs external events unprevented.
