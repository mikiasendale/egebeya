# Debate — E-ai-03 Calendar sync (CEO re-scoped: shared business calendar) — INFRA-BLOCKED
**Surface (declared per persona):** A: merchant SaaS (§4; untagged in §1 INFRA lines) | B: n.d. per row | C: MS | D: merchant SaaS

## 1 · Product Owner
- Two rows in one: (a) **per-staff public ICS subscription URLs** riding the existing one-way feed (`public.ts:1432-1478`) — one-way, "needs zero infra," **NEXT**; (b) the CEO's redefinition (shared staff/owner calendar) is "a *UI build*, not a sync build" — one read-only grid fed by existing `/api/bookings` + role projection; "the single largest small diff in the session," zero infra dependency; unblocks E-bc-08's merchant half, makes E-ms-03 closures visible. **NEXT**. True two-way stays behind the queue. **MED**
- Queue stance (§4): all of Band 1 + most of Band 2 ship BEFORE any infra change; "Phase 0-lite now, Phase 0 proper not" — a 1–2 day libSQL lock-row + BEGIN IMMEDIATE lease around the 7 crons (server.ts:243-310, verified HIGH, no distributed lock); BullMQ+Redis "correct *when the first queue-consumer feature actually arrives*"; Phase 2: stay on libSQL/Turso (Postgres = one-way dialect break), revisit only on measured evidence.

## 2 · Engineering Lead
- **De-classification argument:** "M, no longer infra-blocked." Internal shared view = read of appointments grouped by staff over the E-ms-01 grid; owner-schedules-all = a role tweak on existing gates (bookings.ts:23-41). The **external two-way sync half** remains blocked: durable inbound retry (no queue) + per-provider OAuth (no plumbing) — "it just isn't this ticket anymore." Rule-20 substitute: keep the one-way ICS feed (public.ts:1432-1478) + add per-staff feed variants (S). Crons: none. **HIGH**
- Lease/outbox proposal (§2/§6): Phase 0 as written (BullMQ+Redis+move dispatch) — **reject shape, accept goal**. Replace with **0a: `cron_leases(job_name, window_key)`** marker-first UNIQUE insert (the billingReminders pattern already shipped; 2–4 days; hard prerequisite for the two-instance proof) and **0b: a `job_outbox` table with claim semantics, polled in-process** (M) — durable retry, DLQ-as-status-column, zero new infrastructure. Redis costed: paid always-on worker (free worker sleeps ~50 s cold-start), network hop, doubled secrets surface, at-least-once vs money effects; "workers do not buy throughput against a single-writer libSQL." If calendar push is ever queued: enqueue must be an outbox row **inside the same transaction**; any proposal moving payment-status mutation into a consumer = automatic reject. "Build the lease and the outbox, not the Redis."

## 3 · Council
- **Re-scope GREEN; original two-way sync AMBER behind infra ruling** | MS. Re-scope "strategically correct *because it unblocks a real merchant job … with zero new infrastructure*": E-ms-01's grid + a staff read view over existing appointments/staff_availability.
- Clauses: must derive from appointment columns (AGENTS:21-22) — "do not build a parallel schedule table that becomes a second truth"; the re-scoped UI "must not promise sync it can't do" — publish-only feed copy stays honest. Collision: NO if it rides appointments; **YES if it forks a second calendar truth**. **HIGH**
- Stack-switch floor conditions (C §6, recorded on this row): verdict **queue+worker CONDITIONAL YES; Postgres NOT YET; order matters more than the answer**. Must-not-regress acceptance: (1) webhook idempotency — processed_webhook_events dedupe + raw transactional update, gate test green every phase + new double-delivery case; (2) inline eq(tenantId) law + tenant_id on the job ledger (Gap.md:440-448 "as law, not suggestion"); (3) single-writer topology — "web enqueues, workers write" preserves it; forbidden intermediate = web-and-workers-both-write-directly; no two web instances with in-memory limiters or double-fired crons; (4) cron idempotency markers preserved — "a distributed lock WITHOUT these is strictly worse than today's unlocked double-run"; Phase-0 proof is a **test, not a demo**; (5) channels never throw — dead Redis degrades to logged failure, enqueue must not fail the request (web <500ms); (6) money-gate semantics frozen during any migration window. Standing precondition: render.yaml still ships ALLOW_UNVERIFIED_PAYMENTS="true" — money integrity before money features. "If the CEO defers infra entirely: the rule-20 substitutes stand as written."

## 4 · Support/CRM
- **"The single most ask-shaped line in the Gap file. Impact: HIGH. Build this season's cheapest version now."**
- The re-scope is "two sentences a real Addis merchant *already says* into Telegram every week" (owner typing "ስለዚህ ቀን አለህ?"; stylist calling to ask if they're on — the E-ms-02 workaround, Gap.md:184). Internal calendar "needs no new infrastructure at all": staff-role read over staff_availability + appointments. "Every owner on a second chair is currently a human message bus between the calendar and their staff."
- Evidence: DECISION-recorded (CEO re-scope + EGE-ADV surfaces + staff-actor gap Rule 15); the raw *ask* is UNDOCUMENTED (Season 0). Agent sentence: "Your staff see their week on their own phone — no phone calls to you." Day 1: **y**. "Tell the queue/worker debate this is the merchant-facing prize the infra actually unlocks."

## 5 · Challenge round
**A → B:** three shapes for the same migration question — A: a 1–2 day lock-row lease around the 7 crons only, Redis "when the first queue-consumer feature actually arrives"; B: `cron_leases` (2–4 days) **plus** a `job_outbox` polled in-process, and never Redis at 25 tenants; C: accepts the full queue+worker split conditionally, behind six floors. A and B both say "no Redis now" and disagree on what "now" includes.
**B → C:** B prices the CEO plan's web+queue+workers diagram "mispriced for this one" and wants the queue self-funding (Phase 3 only); C's floors (test-not-demo, money freeze, markers-before-locks) condition a real split C calls "the right *next* infra" — different answers to "when is infra safe."
**A → C:** A's lease is lock-only around crons; C's floor (4) says a distributed lock without the existing marker-first idempotency is "strictly worse than today" — A's 1–2 day unit must carry the billingReminders pattern B cites, or C rejects it.
**C → B:** B's Phase-0a/0b substitutes moot most of C's worker floors for now; C's webhook-atomicity ban and B's same-transaction outbox rule agree in substance but B claims the general case ("automatic reject") C reserves to the CEO.

## 6 · Chair log
- **Agreed:** E-ai-03 is two lines. The shared staff/owner calendar is un-contested ordinary product work (B de-classifies; A Band 2 "largest small diff"; C GREEN; D most-ask-shaped, build before the queue). External two-way sync stays parked with its Rule-20 substitute — one-way per-staff ICS feed URLs — and named trade-off: no protection against double-booking the chair from outside calendars.
- **Contested:** shape and timing of pre-queue migration work (A's lease-only Phase 0-lite vs B's 0a+0b lease+outbox vs C's conditional-yes queue with six floors); B's de-classification vs treating the row as still INFRA-BLOCKED in its original scope; C's second-calendar-truth prohibition binding on any grid implementation choice.
- **Escalation:** ESCALATE: fund and shape the queue+worker migration — Phase 0-lite (A), 0a/0b in-process (B), or conditional full split with C's acceptance floors — and confirm Postgres-not-yet; the stack-switch is a strategy call → CEO (per Checkpoint 1: these are to-dos, CEO final on any migration).
- **Closure state:** AWAITING-CEO
